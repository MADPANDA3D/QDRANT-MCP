services:
  qdrant-mcp:
    build:
      context: .
      args:
        PACKAGE_VERSION: ${PACKAGE_VERSION:-0.0.0}
    container_name: mcp-qdrant
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - mcp-network

networks:
  mcp-network:
    external: true
